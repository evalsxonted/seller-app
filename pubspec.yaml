name: seller
description: A new Flutter application.

# The following line prevents the package from being accidentally published to
# pub.dev using `pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.0.0+4
  path: ^1.8.0
  hive: ^2.0.4
  carousel_slider: ^4.0.0


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/logo.png
    - assets/agreement.png
    - assets/location.png
    - assets/messaging.png
    - assets/shopping.png
    - assets/welcome.png
    - assets/phone.png
    - assets/person.png
    - assets/password.png
    - assets/start.png
    - assets/support.png
    - assets/menu.png
    - assets/menu_rotate.png
    - assets/send.png
    - assets/image.png
    - assets/drawer/account.png
    - assets/drawer/cart.png
    - assets/drawer/delivery.png
    - assets/drawer/heart.png
    - assets/drawer/info.png
    - assets/drawer/logout.png
    - assets/drawer/request.png
    - assets/drawer/products.png
    - assets/drawer/instagram.png
    - assets/drawer/facebook.png
    - assets/icons/location2.png

  fonts:
    - family: Cairo
      fonts:
        - asset: assets/Cairo/Cairo-Black.ttf
        - asset: assets/Cairo/Cairo-Bold.ttf
        - asset: assets/Cairo/Cairo-ExtraLight.ttf
        - asset: assets/Cairo/Cairo-Light.ttf
        - asset: assets/Cairo/Cairo-SemiBold.ttf
        - asset: assets/Cairo/Cairo-Regular.ttf
